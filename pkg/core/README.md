# Core

Domain types (`Task`, `Vault`, etc.) and the data structures used across the rest of the app. Nothing fancy -- just struct definitions, status enums, and validation helpers. The Zig equivalents live in `src/`.
