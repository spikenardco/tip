name: Release
on:
  push:
    tags:
      - "v*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Clone repository
        uses: actions/checkout@v7

      - name: Install Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Test
        run: zig build test --summary all

      - name: Build Windows x86_64
        run: zig build -Dtarget=x86_64-windows -Doptimize=ReleaseSafe

      - name: Rename Windows binary
        run: mv ./zig-out/bin/tip.exe ./zig-out/bin/tip-windows-x86_64.exe

      - name: Build macOS x86_64
        run: zig build -Dtarget=x86_64-macos -Doptimize=ReleaseSafe

      - name: Rename macOS x86_64 binary
        run: mv ./zig-out/bin/tip ./zig-out/bin/tip-macos-x86_64

      - name: Build macOS ARM64
        run: zig build -Dtarget=aarch64-macos -Doptimize=ReleaseSafe

      - name: Rename macOS ARM64 binary
        run: mv ./zig-out/bin/tip ./zig-out/bin/tip-macos-arm64

      - name: Build Linux x86_64
        run: zig build -Dtarget=x86_64-linux -Doptimize=ReleaseSafe -Dcpu=baseline

      - name: Rename Linux x86_64 binary
        run: mv ./zig-out/bin/tip ./zig-out/bin/tip-linux-x86_64

      - name: Build Linux ARM64
        run: zig build -Dtarget=aarch64-linux -Doptimize=ReleaseSafe

      - name: Rename Linux ARM64 binary
        run: mv ./zig-out/bin/tip ./zig-out/bin/tip-linux-arm64

      - name: Generate checksums
        run: |
          cd ./zig-out/bin
          sha256sum tip-windows-x86_64.exe tip-macos-x86_64 tip-macos-arm64 tip-linux-x86_64 tip-linux-arm64 > checksums.txt

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: |
            ./zig-out/bin/tip-windows-x86_64.exe
            ./zig-out/bin/tip-macos-x86_64
            ./zig-out/bin/tip-macos-arm64
            ./zig-out/bin/tip-linux-x86_64
            ./zig-out/bin/tip-linux-arm64
            ./zig-out/bin/checksums.txt
            LICENSE
